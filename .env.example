## Quire MCP environment variables
##
## Copy this file to `.env` and fill in the values.
## NOTE: `.env` is gitignored (do not commit secrets).

# ─────────────────────────────────────────────────────────────────────────────
# Transport selection
# ─────────────────────────────────────────────────────────────────────────────

# Transport mode: "stdio" (default) or "http"
# MCP_TRANSPORT=stdio

# ─────────────────────────────────────────────────────────────────────────────
# Quire OAuth credentials (required for both modes)
# ─────────────────────────────────────────────────────────────────────────────

# Required (Quire developer console → Development Client ID/Secret)
QUIRE_OAUTH_CLIENT_ID=
QUIRE_OAUTH_CLIENT_SECRET=

# ─────────────────────────────────────────────────────────────────────────────
# Stdio mode settings
# ─────────────────────────────────────────────────────────────────────────────

# Redirect URI for interactive OAuth in stdio mode
QUIRE_OAUTH_REDIRECT_URI=http://localhost:3000/callback

# Token cache file path (optional)
# QUIRE_TOKEN_STORE_PATH=

# ─────────────────────────────────────────────────────────────────────────────
# HTTP mode settings (when MCP_TRANSPORT=http)
# ─────────────────────────────────────────────────────────────────────────────

# Host to bind the HTTP server to
# MCP_SERVER_HOST=127.0.0.1

# Port to listen on
# MCP_SERVER_PORT=3001

# Base URL for OAuth endpoints (issuer URL)
# MCP_ISSUER_URL=http://localhost:3001

# Callback URL for Quire OAuth (our server's callback endpoint)
# This must be registered in Quire developer console
# QUIRE_OAUTH_REDIRECT_URI=http://localhost:3001/oauth/callback
