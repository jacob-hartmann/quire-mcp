## Quire MCP environment variables
##
## Copy this file to `.env` and fill in the values.
## NOTE: `.env` is gitignored (do not commit secrets).

# ─────────────────────────────────────────────────────────────────────────────
# Transport selection
# ─────────────────────────────────────────────────────────────────────────────

# Transport mode: "stdio" (default) or "http"
#
# - stdio: MCP client spawns the server. Communicate via stdin/stdout.
#          Use this with Claude Desktop, Claude Code, Cursor, etc.
#
# - http: Run as HTTP server. Client connects to /mcp endpoint.
#         Server must be running before clients connect.
#
# MCP_TRANSPORT=stdio

# ─────────────────────────────────────────────────────────────────────────────
# Quire OAuth credentials (required for both modes)
# ─────────────────────────────────────────────────────────────────────────────

# Get these from: https://quire.io/apps/dev
# Required unless QUIRE_ACCESS_TOKEN is set.
QUIRE_OAUTH_CLIENT_ID=
QUIRE_OAUTH_CLIENT_SECRET=

# ─────────────────────────────────────────────────────────────────────────────
# Manual token override (optional, skips OAuth entirely)
# ─────────────────────────────────────────────────────────────────────────────

# If set, OAuth is bypassed entirely. The token won't auto-refresh.
# QUIRE_ACCESS_TOKEN=

# ─────────────────────────────────────────────────────────────────────────────
# STDIO mode settings (MCP_TRANSPORT=stdio or not set)
# ─────────────────────────────────────────────────────────────────────────────

# Redirect URI for interactive OAuth flow.
# The server creates a temporary HTTP server on this URL to receive the callback.
# Must match the redirect URL configured in your Quire app.
QUIRE_OAUTH_REDIRECT_URI=http://localhost:3000/callback

# Token cache file path (optional, uses platform default if not set)
# Default locations:
#   Windows: %APPDATA%\quire-mcp\tokens.json
#   macOS:   ~/Library/Application Support/quire-mcp/tokens.json
#   Linux:   ~/.config/quire-mcp/tokens.json
# QUIRE_TOKEN_STORE_PATH=

# ─────────────────────────────────────────────────────────────────────────────
# HTTP mode settings (MCP_TRANSPORT=http)
# ─────────────────────────────────────────────────────────────────────────────

# Host to bind the HTTP server to
# MCP_SERVER_HOST=127.0.0.1

# Port to listen on
# MCP_SERVER_PORT=3001

# Base URL for OAuth endpoints (issuer URL)
# This should match how clients will access the server
# MCP_ISSUER_URL=http://localhost:3001

# Callback URL for Quire OAuth (our server's callback endpoint)
# This must be registered in your Quire app's redirect URL settings.
# For HTTP mode, use the server's /oauth/callback endpoint.
# QUIRE_OAUTH_REDIRECT_URI=http://localhost:3001/oauth/callback
